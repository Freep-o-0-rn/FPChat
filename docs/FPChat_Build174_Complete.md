# FPChat Build 174 — реализация, проверки и измерения

20.09.2026. Ветка: `build/174-development`.
База сравнения и отката: завершённая кодовая доработка 173,
`860e303f16af8eafbf7526953d262e481d037d2f`.
Стабильная `main`: Build 168, `9c53a7a0329f267a51c7e976f1c4c825d9b7ae42`.

## Статус и границы

Код 174 реализован, автоматическая проверка 169–174 и три браузерные регрессии пройдены. Переход к 174 и публикация отдельной ветки выполнены по прямому указанию пользователя после доработки 173. Ручная приёмка на слабом Android и iPhone PWA и полный baseline физических устройств остаются открытыми. Поэтому исходный критерий «задержки уменьшаются на слабом устройстве» ещё не объявляется выполненным. Build 175 не начата.

Соблюдён принцип из конца CHANGELOG: изменения встраиваются в существующие механики и владельцев. Шифрование, SQL-схема, retry/clientMessageId и формат persistent storage сохранены. После аудита upload API получил необязательный `uploadId` и удаление pending по `uploadIds`: это позволяет убрать файл при отмене после server commit, даже если ответ XHR ещё не дошёл. Старый запрос без этих полей сохраняется. Новая persistent queue не создаётся.

Исправления накопительной версии 170–174 после внешнего аудита описаны в [отдельном отчёте](FPChat_Audit_170-174.md). Два новых сообщения пользователя о микрофоне и открытии drawer включены в план 175, ещё не исправлены.

## Что изменено

1. `renderChats` сохраняет строки по roomId, обновляет только изменившуюся разметку, переставляет существующие строки и объединяет быстрые повторные запросы. Обработчики click/long-press остаются у той же строки. Поиск отменяет устаревший проход.
2. `FPWork174` обрабатывает создание/удаление строк и историю порциями: до 100 элементов или около 6 мс между проверками. Между порциями отдаёт управление браузеру; async-элементы выполняются последовательно. Это мягкий бюджет между элементами, не гарантия максимальной длительности отдельного элемента. `maxSliceMs` включает время ожидания async, это не чистое CPU-время.
3. Критические startup-зависимости описаны в `startupDependencies174` в `index.html`. Preload переносит загрузку части ресурсов вперёд, но не меняет порядок исполнения. Карта охватывает изменённые критические цепочки, не является полным графом всех исторических модулей.
4. `FPHistory174` загружает окно около anchor двумя существующими запросами `before/after`, поддерживает движение в обе стороны и выгружает дальние DOM-узлы. Переходы reply, pins, unread и к концу используют тот же путь. Устаревшие страницы и смена комнаты отменяются/игнорируются.
5. Выгрузка DOM не удаляет каноническое сообщение или выделенные id. Read/status сохраняются в MessageStore; старые страницы не отменяют edit/delete. Новый incoming при чтении старой истории учитывается без добавления узла через разрыв истории. Отправка из старой истории сохраняет optimistic node и возвращает к концу без дублирования ACK.
6. Исправлены ранняя установка unread divider при порционной отрисовке, оставшийся разделитель выгруженного anchor и переход к первому непрочитанному вне окна. Cursor продвигается даже через страницу, все записи которой уже tombstones. Поздний thumbnail освобождает URL вместо привязки к удалённому узлу.

## Владельцы и ограничения окна

| Ресурс | Владелец / поведение |
|---|---|
| Room/key/generation/cancel | Существующий `FPRoomContext170`; запрос окна дополнительно отменяет предыдущую навигацию того же окна |
| Fetch/XHR, media/cache | Существующий `FPNetwork171`; история использует его глобальный fetch, второго транспорта или cache writer нет |
| Канонические данные, reply, статусы | `FPMessageStore172`; DOM не становится вторым хранилищем |
| Scroll/viewport/gesture/layers/DOM events | `FPScroll173`, `FPViewport173`, `FPGesture135`, `FPLayer173`, `FPDOM173`; история пишет scroll через существующего владельца |
| Рабочий диапазон истории | `FPHistory174`: page 100, установившееся окно до 300 сообщений, включая pending |
| Диагностика | `FPHistory174.snapshot()`, `FPChatList174.snapshot()`, `FPWork174.snapshot()`; coverage `FPRuntime` остаётся частичным |

Страница сначала рендерится отдельно, затем синхронно сверяется с MessageStore, монтируется и обрезается до 300 сообщений. Pending также выгружаются: их канонические записи и существующая retry-очередь сохраняются, прокрутка восстанавливает DOM из Store без нового транспорта. Проверен сценарий 420 отправок без ACK, возврат к первой и ACK после выгрузки.

300 — предел установившегося списка сообщений, а не всех элементов документа: во время операции есть подготовленная страница (100, около anchor — до 200); при замене хвоста учитываются до 300 pending. Временные деревья после операции освобождаются. Размер существующей retry-очереди не ограничивается этим DOM-бюджетом; новый persistent storage не добавлен. При pruning Store теперь защищены исходные сообщения reply, проверено после 10 010 записей.

При отказе загрузки `history174.js` включается существующая legacy history: клиент доступен, ошибка отражается в `FPBoot152.errors()`, но bounded DOM в этом аварийном режиме не обещается. При отказе `work174.js` используется последовательный fallback без гарантии порционного yield. Эти пути не работают параллельно с новым владельцем в штатном режиме.

## Startup: порядок выполнения

| Цепочка | Условие |
|---|---|
| network → store → DOM lifecycle → layers → work/history → RoomContext/Lifecycle → app definitions | Владельцы доступны до использования в app |
| app → typing → voice → voice polish | Сохранены существующие onload-цепочки |
| app → selection → context → context fix → actions → pins → pins screen | Сохранены существующие onload-цепочки; дальнейшие дополнения остаются в их загрузчике |
| app/settings → room lifecycle → room-open170 | Явное событие готовности: скорость preload не определяет порядок обёрток |
| room-open170 → text-send170 → media-send170 | Startup IIFE ждёт готовности всей цепочки до прямого входа в комнату или invite |

После CSS hints заранее загружаются 11 ресурсов: RoomContext, Lifecycle, network, store, DOM lifecycle, layers, work/history, app, settings-fix и room-lifecycle. `app.js` сначала устанавливает определения, затем ждёт Promise готовности connection/room-open/text/media. Прямые `/chat/...` и `/i/...` проверены с задержкой room-open: один join, актуальный контекст. Повторный repair-open из room-lifecycle удалён. Отказ room-open показывает повтор загрузки; старый незащищённый вход не запускается. Карта не объявляется полным dependency resolver всех исторических скриптов.

## Воспроизводимые измерения

Таблица ниже — историческое сравнение 173 и первоначальной 174 (`33f6c00`), до исправлений аудита.

Среда: Linux, headless Chromium 153.0.8010.0, runner Node 24.19.0, сервер Node 22.20.0, production bootstrap и временная SQLite. Три независимых браузерных профиля на вариант; сравнение проводится последовательно, без параллельного браузерного regression runner. Это локальный стенд, не физический телефон и не сетевой benchmark.

| Сценарий | 173: медиана / худший, мс | 174: медиана / худший, мс |
|---|---:|---:|
| Открытие 1500 сообщений около сохранённого 21-го | 4116,5 / 4237,3 | 455,0 / 455,3 |
| 20 подряд обновлений одной строки из 500 | 1086,0 / 1232,9 | 46,3 / 47,7 |
| Первое заполнение списка из 500 строк | 180,1 / 182,7 | 199,3 / 248,3 |
| Boot-ready пустого нового профиля | 1063 / 1176 | 1041 / 1076 |

В сценарии истории: 14 → 2 дополнительных history-запроса, 1500 → 121 смонтированное сообщение, 12237 → 1212 DOM-элементов документа. Anchor присутствует во всех шести прогонах. При 20 обновлениях списка 173 удаляет и создаёт 10000 строк; 174 сохраняет все 500 строк и не заменяет их корневые узлы.

Первое полное заполнение списка в 174 медленнее: порции дают браузеру обработать промежуточную работу. Это зафиксированный компромисс, а не улучшение всех метрик. В тесте списка после завершения ждём два animation frames для обеих версий; в тесте истории — завершения `openChat` и следующий frame. Ни один тест не измеряет полное GPU-представление кадра, энергопотребление или поведение на слабом Android. Ранние разведочные прогоны с одним frame и другой нагрузкой стенда не смешаны с итоговой таблицей.

Сырые данные: [list 173](benchmarks/build174-list-173.json), [list 174](benchmarks/build174-list-174.json), [history 173](benchmarks/build174-history-173.json), [history 174](benchmarks/build174-history-174.json).

Повторный прогон после исправлений аудита, по три свежих профиля, без параллельных тестов: открытие истории — медиана **180,2 мс**, худший **180,5 мс**, 121 сообщение, anchor присутствует; счётчик за весь сценарий зафиксировал **3** дополнительных запроса истории. 20 обновлений строки — **49,8 / 50,0 мс**, сохранены все 500 строк; первый render — **64,4 / 97,2 мс**, boot-ready — **535 / 576 мс**. [История](benchmarks/build174-history-audit.json), [список](benchmarks/build174-list-audit.json). Старую 173 одновременно повторно не замеряли, поэтому изменение задержек относительно прежней таблицы нельзя целиком приписывать патчу. Это не мобильный baseline.

## Регрессия

`npm run check:169-174`: все шесть наборов PASS. Сохранены VM/static проверки 169–173; 174 дополнительно исполняет отменяемые последовательные порции, проверяет распространение ошибок, отсутствие циклов в startup map и синтаксис public/inline JS. Это не замена браузерному UX-тесту.

`npm run test:173:browser`: **17 PASS, 0 FAIL**, прежняя совместная регрессия 169–173. Искусственная задержка anchor теперь распознаёт и старый курсор, и новый запрос около anchor; сами утверждения о позднем результате сохранены.

`npm run test:174:audit`: **18 PASS, 0 FAIL** — реальные upload/cancel, гонки decrypt/mount, pruning, direct startup, жесты, lifecycle, streaming, pending DOM и совместимость upload API; подробности в отчёте аудита.

`npm run test:174:browser`: **17 PASS, 0 FAIL**:

1. Сохранение строк, точечное обновление и отмена устаревшего поиска.
2. Yield и отмена порции.
3. Далёкий сохранённый anchor: два запроса, offset с допуском 2 px.
4. Шесть страниц назад и шесть вперёд: variable-height сообщения, предел 300, дрейф anchor до 2 px.
5. Reply после выгрузки, canonical edit и tombstone.
6. Реальный server pin и переход к далёкому сообщению.
7. Выделение после выгрузки/возврата и удаление offscreen сообщения через API.
8. Отправка из старой истории, один подтверждённый узел и переход к концу.
9. Продвижение через полностью tombstoned страницу.
10. Поздняя anchor-загрузка A не меняет B.
11. Первое непрочитанное далеко от конца видно; скрытый хвост не становится read.
12. Кнопка unread загружает отсутствующий в DOM первый unread; нет orphan divider.
13. Incoming в старом окне не создаёт DOM-разрыв и не сдвигает anchor.
14. Thumbnail после выгрузки освобождает URL и не возвращает его в старый узел.
15. Задержанный room-lifecycle при старте и по одной копии владельцев.
16. Отказ history asset оставляет доступный клиент с зарегистрированной ошибкой.
17. Нет необработанных исключений браузера.

В fixtures: 1500 собственных сообщений разной высоты, короткая комната и 1200 входящих с первыми 200 прочитанными. Incoming race и thumbnail race управляемые; последний не заменяет тест реальной загрузки фото. Реальный XHR upload/progress/cancel теперь включён в audit suite с тестовым PNG и временной БД. Voice/microphone/autoplay, push, реальные suspend/resume и физические жесты/клавиатура требуют отдельной приёмки. Синтетические события lifecycle/touch не заменяют устройства. Длительная память всего процесса не измерялась.

### Команды

Нужны установленные серверные зависимости, Playwright и Chromium. Серверный Node должен быть совместим с native `better-sqlite3`. В отдельной среде пути задаются через `NODE_PATH`, `FPCHAT_TEST_NODE`, `BROWSER_EXECUTABLE_PATH`; при обычной установке используется текущий Node и Chromium Playwright.

```sh
npm run check:169-174
npm run test:173:browser
npm run test:174:browser
npm run test:174:audit
FPCHAT_BENCH_OUTPUT=/tmp/list174.json npm run bench:174:list
FPCHAT_BENCH_OUTPUT=/tmp/history174.json npm run bench:174:history
```

Для сравнения неизменённой 173 создать отдельный checkout и запускать те же benchmark scripts из 174:

```sh
git worktree add --detach /tmp/fpchat-173 860e303f16af8eafbf7526953d262e481d037d2f
FPCHAT_TEST_ROOT=/tmp/fpchat-173 FPCHAT_BENCH_OUTPUT=/tmp/list173.json npm run bench:174:list
FPCHAT_TEST_ROOT=/tmp/fpchat-173 FPCHAT_BENCH_OUTPUT=/tmp/history173.json npm run bench:174:history
```

Benchmark scripts запускают реальный сервер на свободном локальном порту, используют только свою временную SQLite, закрывают процессы и удаляют fixture. Рабочая БД и push-подписки не используются. В обоих checkout должны быть доступны серверные зависимости, в том числе через общий `NODE_PATH`.

## Откат

174 не содержит миграций БД и изменений форматов очереди/кэша. Для отката только исправлений аудита использовать `5e5a9e0bbfcb9e393b5d4479e6ce4de0e6c0ea4e` (код первоначальной 174 + пользовательский README); при этом возвращаются исправленные ошибки. Обновлять клиент и сервер вместе, затем перезагрузить открытые страницы: номер сборки остаётся 174, автоматическое обновление только по увеличению build не сработает. Точка возврата — вся ревизия 173 `860e303f16af8eafbf7526953d262e481d037d2f`, а не отдельное удаление `history174.js`.

Для локального стенда остановить процесс, переключить чистый checkout на эту ревизию (`git switch --detach 860e303f16af8eafbf7526953d262e481d037d2f`) и запустить штатным `npm start`. `version.json` вернёт suffix 173; затем полностью перезапустить клиент и проверить version, отправку и открытие anchor. На общей ветке делать обычный revert коммита 174, без переписывания истории. `update.bat` в этой сборке меняет только номер banner; логика updater/startup относится к 175 и здесь не переделывается.
